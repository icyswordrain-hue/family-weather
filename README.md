# 🌤️ Family Weather Dashboard

> **Last updated: 2026-03-12**

A private, AI-narrated daily weather broadcast for a family in Shulin/Sanxia/Banqiao (New Taipei City, Taiwan). Each day the app fetches live weather and air quality data, generates a natural-language narration via an LLM (Claude or Gemini), synthesises it to speech, and surfaces everything in a responsive dashboard.

---

## Features

| Feature | Details |
|---|---|
| **Live weather data** | Current conditions + 36-hour & 7-day forecasts via [CWA Open Data](https://opendata.cwa.gov.tw/) |
| **Air quality** | Real-time AQI + 3-day regional forecast via [MOENV](https://data.moenv.gov.tw/) |
| **AI narration** | Daily broadcast script generated by Claude (Sonnet 4.6/Haiku 4.5) or Gemini (2.0 Pro/Flash), with template fallback |
| **Text-to-Speech** | Audio rendered by Google Cloud TTS (`zh-TW-Wavenet-B` / `en-US-Neural2-D`) or Microsoft Edge TTS (offline fallback) |
| **Outdoor Score** | Rule-engine score (0–100, A–F grade) rating suitability for outdoor activity based on temp, humidity, AQI, UV, and visibility |
| **Health & Safety Alerts** | Early warnings for Cardiac risk (cold/wet swings) and Ménière's disease triggers (barometric drops) |
| **Lifestyle Advice** | Weather-driven meal suggestions, commuter hazard warnings (rain/wind), HVAC mode and wardrobing tips |
| **AI Chat** | Contextual weather Q&A powered by Claude Haiku 4.5 with today's broadcast as context |
| **Language toggle** | English / Traditional Chinese (`zh-TW`) — narration, UI labels, and TTS voice all switch together |
| **Brand Icons** | Custom fine-line sketch illustrations (`web/static/brand-icons/`, WebP only) replace all emoji in the UI — weather conditions, lifestyle categories, alert states |
| **Earthy Theme** | Custom "earthy sophistication" colour palette — terracotta, sand, sage, dusty plum; light mode (dark mode planned) |
| **PWA** | Service worker + web manifest for installable offline use |
| **Streaming refresh** | Pipeline progress streamed to the browser via NDJSON (Server-Sent Events) |
| **Run slots** | Smart morning / midday / evening slots; midday run skipped if conditions unchanged from morning |
| **Narration cache** | 30-minute in-memory cache keyed by language + city + weather condition + time-of-day |
| **14-day regen cycle** | Meal & location database auto-regenerated by the LLM every two weeks |

---

## Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                     Browser (PWA)                            │
│  dashboard.html  ←  app.js  +  style.css                     │
└──────────────────┬─────────────────────────────┬────────────┘
                   │ /api/refresh (NDJSON stream) │ /api/broadcast
                   ▼                             ▼
┌─────────────────────────────────────────────────────────────┐
│                    Flask (app.py)                            │
│  LOCAL / MODAL mode: runs pipeline directly                  │
│  CLOUD mode: proxies to Modal serverless endpoints           │
└──────┬──────────────┬──────────────┬────────────────────────┘
       │              │              │
  data/          narration/      history/
  fetch_cwa      llm_prompt_     conversation.json
  fetch_moenv    builder         (local file or GCS)
  weather_       claude_client
  processor      gemini_client
  outdoor_score  tts_client
  health_alerts  chat_context
```

### Run modes (`RUN_MODE` env var)

| Mode | Description |
|---|---|
| `LOCAL` | Flask dev server runs the full pipeline in-process. Reads/writes `local_data/`. |
| `CLOUD` | Flask acts as a thin proxy; the pipeline runs on [Modal](https://modal.com/) serverless workers which write to GCS. |
| `MODAL` | Used inside Modal workers themselves; reads/writes to a mounted Modal Volume at `/data`. |

---

## Project Layout

```
family-weather/
├── app.py                  # Flask app & pipeline orchestration
├── config.py               # All config & env var loading
├── Dockerfile              # Production container (gunicorn, Python 3.12)
├── requirements.txt
├── requirements-modal.txt  # Modal-specific dependencies
├── run_local.ps1           # Quick local dev start (Windows)
├── run_cloud.ps1           # Cloud deploy helper
│
├── data/                   # Data fetching & processing
│   ├── fetch_cwa.py        # CWA weather API client
│   ├── fetch_moenv.py      # MOENV AQI API client
│   ├── health_alerts.py    # Cardiovascular and Ménière's health alerts
│   ├── helpers.py          # Common processing helpers
│   ├── location_loader.py  # Utility for loading location data
│   ├── locations.json      # Structured location metadata
│   ├── meal_classifier.py
│   ├── outdoor_scoring.py  # Rule-engine outdoor score (A–F)
│   ├── scales.py           # Scoring and index scales
│   ├── station_history.py  # Rolling 7-day station observation history
│   └── weather_processor.py
│
├── narration/              # AI narration pipeline
│   ├── chat_context.py     # ~650-token system prompt for /api/chat
│   ├── claude_client.py    # Sonnet 4.6 primary, Haiku 4.5 fallback
│   ├── fallback_narrator.py    # Template-based fallback
│   ├── gemini_client.py
│   ├── llm_prompt_builder.py   # v7 prompt construction & response parsing
│   └── tts_client.py           # Google Cloud TTS / Edge TTS
│
├── backend/
│   ├── pipeline.py         # Isolated business logic (testable)
│   ├── cache.py            # In-memory narration cache
│   └── modal_app.py        # Modal serverless entry points
│
├── web/
│   ├── routes.py           # build_slices() — per-view payloads
│   ├── templates/dashboard.html
│   ├── static/app.js       # All frontend: views, player bar, chat, language toggle
│   ├── static/style.css    # Earthy design system, responsive
│   ├── static/manifest.json
│   ├── static/service-worker.js
│   └── static/brand-icons/ # Fine-line sketch illustrations (WebP only)
│
├── history/
│   └── conversation.py     # Read/write daily broadcast history
│
├── tests/                  # pytest suite
└── docs/plans/             # Design docs & implementation plans
```

---

## Setup

### Prerequisites

- Python 3.12+
- A [CWA Open Data](https://opendata.cwa.gov.tw/) API key
- A [MOENV](https://data.moenv.gov.tw/) API key
- An [Anthropic](https://console.anthropic.com/) API key (Claude)
- A [Google AI Studio](https://aistudio.google.com/) API key (Gemini)
- *(Optional)* A GCP service account with **Cloud TTS** and **Cloud Storage** permissions, for Google TTS and cloud history storage.

### 1. Clone & install

```powershell
git clone <repo-url>
cd family-weather
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

### 2. Configure environment

Copy `.env` and fill in your keys:

```dotenv
RUN_MODE=LOCAL

CWA_API_KEY=<your-key>
MOENV_API_KEY=<your-key>
GEMINI_API_KEY=<your-key>
ANTHROPIC_API_KEY=<your-key>

# Optional: override narration provider (CLAUDE | GEMINI | TEMPLATE)
NARRATION_PROVIDER=CLAUDE

# Optional: GCP for Cloud TTS & GCS history
# GCP_PROJECT_ID=...
# GCS_BUCKET_NAME=family-weather-dashboard
# GOOGLE_APPLICATION_CREDENTIALS=local_data/service-account.json
```

> For Google Cloud TTS, place your service account JSON at `local_data/service-account.json`. If absent, the app falls back to **Microsoft Edge TTS** (no credentials required).

### 3. Run locally

```powershell
.\run_local.ps1
# or:
$env:RUN_MODE = "LOCAL"; python app.py
```

Navigate to `http://localhost:8080`.

---

## Environment Variables Reference

| Variable | Default | Description |
|---|---|---|
| `RUN_MODE` | `CLOUD` | `LOCAL`, `CLOUD`, or `MODAL` |
| `CWA_API_KEY` | *(required)* | CWA Open Data API key |
| `MOENV_API_KEY` | *(required)* | MOENV AQI API key |
| `GEMINI_API_KEY` | *(required)* | Google Gemini API key |
| `ANTHROPIC_API_KEY` | *(required)* | Anthropic Claude API key |
| `NARRATION_PROVIDER` | `CLAUDE` | `CLAUDE`, `GEMINI`, or `TEMPLATE` |
| `GEMINI_PRO_MODEL` | `gemini-2.0-pro-exp` | Gemini model for narration |
| `GEMINI_FLASH_MODEL` | `gemini-2.0-flash` | Gemini Flash model |
| `CLAUDE_MODEL` | `claude-sonnet-4-6` | Claude primary narration model |
| `CLAUDE_FALLBACK_MODEL` | `claude-haiku-4-5-20251001` | Claude narration fallback |
| `CLAUDE_CHAT_MODEL` | `claude-haiku-4-5-20251001` | Claude model for `/api/chat` |
| `CLAUDE_CHAT_MAX_TOKENS` | `300` | Max tokens for chat replies |
| `CHAT_HISTORY_MAX_TURNS` | `6` | Chat turns sent to LLM per request |
| `TTS_PROVIDER` | `GOOGLE` (if creds present) else `EDGE` | `GOOGLE` or `EDGE` |
| `TTS_VOICE_NAME` | `en-US-Neural2-D` | English TTS voice |
| `TTS_VOICE_ZH` | `zh-TW-Wavenet-B` | Chinese TTS voice |
| `GCP_PROJECT_ID` | — | GCP project for Cloud TTS & GCS |
| `GCP_REGION` | `asia-east1` | GCP region |
| `GCS_BUCKET_NAME` | `family-weather-dashboard` | GCS bucket for broadcast history |
| `GCS_AUDIO_PREFIX` | `audio` | GCS prefix for audio files |
| `PORT` | `8080` | Flask / gunicorn port |
| `HISTORY_DAYS` | `3` | Days of history fed to LLM |
| `STATION_HISTORY_DAYS` | `7` | Days of station observations retained |
| `REGEN_CYCLE_DAYS` | `14` | Days between meal/location DB regen |
| `MODAL_REFRESH_URL` | — | Modal refresh endpoint URL (CLOUD mode) |
| `MODAL_BROADCAST_URL` | — | Modal broadcast endpoint URL (CLOUD mode) |
| `MODAL_AUDIO_URL` | — | Modal audio endpoint URL (CLOUD mode) |
| `MODAL_TTS_URL` | — | Modal on-demand TTS endpoint URL (CLOUD mode) |
| `DESKTOP_TTS_URL` | — | Optional external TTS service URL |
| `DESKTOP_TTS_SECRET` | — | Auth secret for external TTS service |
| `DESKTOP_TTS_TIMEOUT` | `8` | Timeout (s) for external TTS service |

---

## Deployment

### Docker / Cloud Run

```powershell
# Build
docker build -t family-weather .

# Run locally via Docker
docker run -p 8080:8080 --env-file .env family-weather
```

Deploy the image to **Google Cloud Run**. Set all env vars as Cloud Run environment variables or via Secret Manager.

### Modal (serverless pipeline)

```powershell
# Deploy the pipeline workers
modal deploy backend/modal_app.py
```

Set your secrets in the Modal dashboard under the name `family-weather-secrets` (matching keys: `CWA_API_KEY`, `MOENV_API_KEY`, `ANTHROPIC_API_KEY`, `GEMINI_API_KEY`).

After deploying, set `MODAL_REFRESH_URL`, `MODAL_BROADCAST_URL`, and `MODAL_AUDIO_URL` on your Cloud Run service to the generated Modal endpoint URLs.

---

## API Endpoints

| Method | Path | Description |
|---|---|---|
| `GET` | `/` | Dashboard UI |
| `GET` | `/api/health` | Health check |
| `GET` | `/api/warmup` | Container warmup check |
| `GET` | `/api/broadcast` | Fetch broadcast data (query: `date=YYYY-MM-DD`, `lang=en\|zh-TW`) |
| `POST` | `/api/refresh` | Run the full pipeline (streaming NDJSON) |
| `POST` | `/api/chat` | Weather Q&A using today's broadcast as context |
| `POST` | `/api/tts` | On-demand TTS synthesis |
| `GET` | `/api/audio/<path>` | Serve TTS audio file |
| `POST` | `/debug/log` | Browser log forwarding (LOCAL only) |

### `POST /api/refresh`

```json
{
  "date": "2026-03-12",
  "provider": "CLAUDE",
  "lang": "zh-TW",
  "slot": "morning"
}
```

`slot` is optional (`morning` / `midday` / `evening`). If omitted, it is inferred from the current Taipei hour. A midday run is automatically skipped if current conditions haven't changed significantly from the morning broadcast.

### `POST /api/chat`

```json
{
  "message": "Will it rain this afternoon?",
  "date": "2026-03-12",
  "lang": "en",
  "messages": []
}
```

`messages` is an optional array of prior turns (up to `CHAT_HISTORY_MAX_TURNS`).

### `POST /api/tts`

```json
{
  "script": "Good morning...",
  "lang": "zh-TW",
  "date": "2026-03-12",
  "slot": "morning"
}
```

---

## Tests

```powershell
pytest tests/
```

---

## Data Sources

- **Weather (home)**: [Central Weather Administration (CWA)](https://opendata.cwa.gov.tw/) — Shulin manual station `72AI40`
- **Weather (work)**: Banqiao auto-station `C0AJ80` (current readings) + Xindian synoptic station `466881` (UV, visibility, pressure)
- **Forecast**: CWA New Taipei City township forecasts — `F-D0047-069` (36h) and `F-D0047-071` (7-day), locations: 樹林區 / 板橋區
- **Air Quality**: [Ministry of Environment (MOENV)](https://data.moenv.gov.tw/) — Tucheng station `土城`, Northern zone 3-day forecast
- **Location**: Shulin District, New Taipei City (24.9955°N, 121.4279°E), `Asia/Taipei`

---

## Changelog

### 2026-03-07 — 36h Segment Row Refinements (`27ca251`)

**Files:** `web/static/app.js`, `web/static/style.css`

| # | Change | Detail |
|---|---|---|
| 1 | **Height 3/4** | `min-height` reduced 168 → 126 px (desktop), 128 → 96 px (mobile); padding tightened to match |
| 2 | **Aligned temp gauge** | Bar positions already anchored to global AT min/max across all segments — no change needed; documented for clarity |
| 3 | **2× icon watermark** | Right-column stat icons rendered as `position: absolute` at 40 px with `opacity: 0.18`, text sits on top via `z-index: 1`; uses new `.tc-stat-icon` / `.tc-stat-text` wrapper elements |
| 4 | **Conditional outdoor / PoP** | Day segments (h < 18 and h ≥ 6) show outdoor grade; overnight segments show PoP (precipitation probability) |
| 5 | **Alert-style transition card** | `.tc-transition` restyled as a flex card with `border-left`, `box-shadow`, and a structured icon + `CHANGE` label + detail-text layout, matching the lifestyle alert card pattern |

### 2026-03-07 — AT Double-Count Fix (`4052454`)

**File:** `data/weather_processor.py`

CWA's 36-hour API already returns a pre-computed apparent temperature (`AT`). The processor was additionally running the BOM formula (using `T`, `RH`, `WS`) to produce a `feels_like` value and overwriting `AT` with it — effectively double-counting humidity and wind. The formula application is now removed; `AT` from the API is preserved as-is, matching the policy already applied to 7-day segment slots.
